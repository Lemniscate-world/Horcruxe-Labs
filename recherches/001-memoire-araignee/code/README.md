# Code — Mémoire Araignée (labo)

Provenance : copies exactes du 2026-09-20 depuis russel-agent, pour travailler ici sans casser l'original.

- `spider_cache.py` ← `C:\Users\Utilisateur\Documents\russel-agent\memory\spider_cache.py`
- `../tests/test_spider_cache.py` ← `C:\Users\Utilisateur\Documents\russel-agent\tests\test_spider_cache.py`

Dépendances minimales : `numpy`, `pytest`, optionnel `sentence-transformers` (fallback sans embedding), optionnel `watchdog`.

Lancer tests (depuis ce dossier) :
```
python -m pytest ../tests/test_spider_cache.py -v
```

Prochain code labo : `bench_latency.py`, `weaver.py` (Toile 4) — à créer ici.
